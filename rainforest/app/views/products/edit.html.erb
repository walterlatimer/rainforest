<h1>Editing product</h1>

<%= render 'form' %>

<!-- Display thumb of image while editing and added a break -->
<%= image_tag @product.photo.url(:thumb) %>
<br>


<%= link_to 'Show', @product %> |
<%= link_to 'Back', products_path %>
